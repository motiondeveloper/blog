---
title: eBox Code Example
---

```js
const myBox =
  eBox.createBox({
    size: [200, 200],
    position: [0, 0],
    anchor: 'center',
  });

myBox.setScale(
  [scaleIn, 100],
  'topLeft’
);

myBox.getPath();
```
